# ============================================================
# 白名單制，不是黑名單制。
# 這個 repo 會公開到 GitHub public，出錯的代價是敏感資料外洩，
# 所以先擋掉「所有東西」，再一項一項放行要公開的內容。
# 做法與 hwc-roster-automation 完全一致。
# ============================================================

# 擋掉根目錄下所有檔案與資料夾
/*

# 明確放行
!/src/
!/docs/
!/tests/
!/tools/
!/.gitignore
!/README.md
# 一鍵部署批次檔。檔案內**沒有**任何 ID——部署 ID 另外放在
# deployment-id.txt（見下面「不可公開」那一節），所以這個檔案可以公開。
!/deploy.bat

# ------------------------------------------------------------
# 以下明確列出「已知」不可公開的項目。
# 即使前面 /* 已經擋住，這裡再寫一次是防呆：
# 日後有人改壞前面的規則時，這份清單仍然是提醒。
# ------------------------------------------------------------

# .clasp.json：內含 Apps Script 專案的 scriptId
.clasp.json
.clasprc.json

# deployment-id.txt：deploy.bat 要用的 Web App 部署 ID。
# 前面的 /* 已經擋住，這裡再寫一次是防呆——這個檔案永遠不可以公開。
deployment-id.txt

# 憑證與金鑰
*.pem
*.key
credentials*.json
client_secret*.json
service-account*.json
token.json

# 真實資料匯出檔（週報樣本、職事表匯出、名單、電郵清單）
*.csv
*.xlsx
*.docx
*.pdf
/samples/
/data/
/private/

# 編輯器與系統檔
.DS_Store
Thumbs.db
.vscode/
.idea/
node_modules/

# ⚠️ 日後在 tools/ 或 docs/ 新增檔案時記住：這兩個資料夾**會公開**。
# 任何含真實姓名、電郵、試算表 ID、資料夾 ID 的內容，一律不可放進去。
