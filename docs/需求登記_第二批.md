# 需求登記（新增第二批）R-030 至 R-037

2026-08-25 加入。每一條的設計理據與考證結果寫在下面。

---

## R-030　夏令時間家事報告自動加入　【已完成】（2026-08-26）

### 考證結果（由 88 期真實週報抽出）

只找到三則，格式穩定：

> **2025-03-30**（夏令時間完結前一週）
> 今年標準時間將於下主日(4月6日)正式開始。請各位嘉賓、朋友和弟兄姊妹於本週六晚將時間回撥一小時。

> **2026-03-29**（夏令時間完結前一週）
> 今年Daylight Saving於下主日(4月5日) 完結，請大家於本週六晚將時間回撥一小時。

> **2025-09-21**（夏令時間開始前一週）
> Daylight Saving將開始：今年Daylight Saving於下主日(28/9)開始，請於27/9星期六晚上將時鐘撥前一小時 (如圖)。

### 規則

紐西蘭夏令時間：**9 月最後一個主日開始，4 月第一個主日完結**。
提示一律登在**改動當日的前一個主日**那一期。一年兩次。

### 設計

Config 兩條範本，可改：

| 鍵 | 預設值 |
|---|---|
| `DST_START_ANNOUNCEMENT` | `Daylight Saving將開始：今年Daylight Saving於下主日({{CHANGE_DATE}})開始，請於{{SATURDAY}}星期六晚上將時鐘撥前一小時。` |
| `DST_END_ANNOUNCEMENT` | `今年Daylight Saving於下主日({{CHANGE_DATE}}) 完結，請大家於本週六晚將時間回撥一小時。` |
| `DST_ANNOUNCEMENT_SEQ` | `5`（排在家事報告的次序，數字細者在前） |
| `DST_AUTO_INSERT` | `TRUE` |

- 建立該季週報時，**由日期計算**出該季有沒有夏令時間轉換的前一個主日，
  有就自動寫入內容表的 `家事報告`，`SEQ_NO` 用 `DST_ANNOUNCEMENT_SEQ`
- **不要硬寫日期表**。用「9 月最後一個主日」「4 月第一個主日」計出來，
  這樣 2030 年都仍然啱
- 寫入之後由使用者自行修改或把「有效」改 `FALSE`，系統不再覆寫
  （加一欄 `SOURCE=SYSTEM_DST` 標明來源，重建季度時只覆寫未被人手改過的）
- `{{CHANGE_DATE}}`、`{{SATURDAY}}` 的格式跟隨現有日期格式設定

⚠️ 2025-09-21 那則原文有「(如圖)」，指週報有一張時鐘圖。範本預設**不含**這句，
需要圖就由幹事自己在內容表加，因為系統不處理圖片。

---

## R-031　誦讀按四個月輪替　【已完成，已考證】

`Config` 已有 `RECITATION_JAN_APR = 使徒信經`、`RECITATION_MAY_AUG = 十誡`、
`RECITATION_SEP_DEC = 主禱文`，分組由 `RECITATION_MONTH_GROUPS` 控制，
`BulletinWeeks` 有逐週的「誦讀（覆寫）」欄。

**考證**：抽出 2025-01-05 至 2026-08-23 共 83 期的誦讀內容，
1-4 月全部使徒信經、5-8 月全部十誡、9-12 月全部主禱文，分界一天不差。
現有設定與實際完全吻合，不需要改動。

---

## R-032　內容過多時的分頁規則　【已完成】（2026-08-27，只做提示，不做自動搬版）

### 現時知道的

由 88 期的內容量分析：

- 中位數約 4,500 位元組，最長的幾期約 6,400，多出約四成
- 最長那幾期：`2026-02-22`、`2025-08-10`、`2026-07-19`、`2025-11-02`、`2025-12-14`
- 撐大的主因有三個：
  1. 家事報告條數多，而且每條有自己的小標題
     （`2025-11-02` 有 10 條：年標、NCD 報告、堂委主席安排、培訓報告、換屋頂、
     財務部消息、報佳音、短宣報名、社區講座、肢體消息）
  2. 代禱事項每條有小標題（同期 5 條）
  3. 兩張財政表同期出現（綜合收支 ＋ 特殊海外奉獻及慈惠）

### 做不到的

Project files 是純文字擷取，**抽不到頁數**，所以我無法確認哪幾期真的是 5 頁，
更無法看出第 5 頁放了什麼。

### 需要的

在 Drive 找出真正 5 頁或以上的那幾期，上載**真的 .docx 或 PDF**（三份就夠）。
有了真檔案我可以看出：

- 第 5 頁承接的是哪一段（家事報告下半、代禱、團契、還是財政）
- 原本 4 頁保留了什麼
- 分頁是硬分頁符還是自然流動

之後才可以定規則，例如「家事報告超過 N 條就整段移到第 5 頁」，
或者「財政表兩張同期出現時，第二張移到第 5 頁」。

**在有真檔案之前，不要寫任何溢出邏輯。**

---

## R-033　預覽連結 ＋ 每星期一寄給 CC、DB、幹事　【已完成】（2026-08-27）

### 需求

- UI 加一條「預覽」連結，CC、DB 可以看**下一個主日**的最新版本
- 每星期一自動把這條連結寄給 CC、DB、幹事
- 內容是「目前為止的狀態」：職事表抓到的、表單填了的、任何人改過的，
  幹事未必動過手
- **與 master 連結不同**：master 是幹事正式發佈的定稿；預覽是每週都會變的草稿

### 設計

- 另建一個**固定的預覽 PDF 檔案**（與 master 同樣做法，ID 永不變），
  Config `PREVIEW_PDF_FILE_ID`
- 但預覽不能靠幹事用 Word 另存——那正是要避免的。所以預覽用
  **Google Docs 轉換的 PDF**，版面會走樣，接受
- 預覽信與預覽頁面**必須明確標示**：
  `這是草稿預覽，版面與正式印刷版不同，只供核對內容用`
- 星期一觸發器同一支，順手多做一件事：產生預覽、覆寫預覽檔案、寄連結
- Config `PREVIEW_SEND_GROUPS`（預設 `CC,DB,ADMIN`）、`PREVIEW_ENABLED`

⚠️ 決策點：預覽用 Google Docs 轉換是**刻意接受版面走樣**，
因為預覽的用途是核對「講員是誰、家事報告寫了什麼」，不是核對版面。
如果你認為走樣的版面會令堂委誤會，另一個做法是預覽直接給 **.docx 下載連結**，
不做 PDF。兩者揀一個，我建議前者（手機開得到）。

---

## R-034　主日選單改為兩層　【已完成】（2026-08-26）

現時是一條完整日期清單，季度一多就長到不能用。

### 建議

```
季度：[2027T4 ▼]        主日：[2027-11-07 ▼]   [← 上一個] [下一個 →]
```

1. **季度下拉**：只列有資料的季度，預設選中「下一個要寄的主日」所屬的季度
2. **主日下拉**：只列該季的主日，每項後面加狀態標記，例如
   `2027-11-07　已發佈 第 2 版`／`2027-11-14　待填 12 項`
3. **上一個／下一個箭嘴**：跨季度時自動切換季度下拉
4. 開啟 UI 時**預設落在「下一個未發佈的主日」**，不是清單第一項——
   九成情況這就是要做的那一期

狀態標記是重點：幹事最常問的是「邊幾期未搞掂」，把答案放進下拉就不用另外找。

---

## R-035　只保留本季與前一季可見，更早自動封存　【已完成】（2026-08-27，封存＝預設不顯示，一格資料都不刪）

### 設計

Config `RETENTION_QUARTERS_VISIBLE`（預設 `2`，即本季 ＋ 前一季）。

- **不刪任何資料。** `BulletinWeeks` 加一欄 `ARCHIVED`，
  超出保留範圍的季度設 `TRUE`
- UI 的季度下拉預設不顯示已封存的，加一個「顯示已封存」勾選格
- 季度填寫表（`Fill_*`）：超出範圍的隱藏工作表（`hideSheet()`），不刪
- 內容表本身一季一個檔案，天然封存：把 `ContentSheets` 該行 `ACTIVE=FALSE`，
  並把檔案移入 `週報內容表/封存` 子資料夾
- **自動執行時機**：每次「建立本季週報」與每季提示觸發器順手跑一次封存檢查
- 封存動作一律寫 `AuditLog`，並在對話框回報封存了哪幾季

⚠️ 封存不可以令舊資料無法查閱。「顯示已封存」一勾就要看得回，
`Diagnostics` 與 `PublishLog` 完全不受影響。

---

## R-036　職事表沒有該季資料時仍可建立週報　【已完成】（2026-08-26）

### 需求

例如目前是 2029T1 而職事表未有該季，使用者仍然可以建立該季週報，
事奉格留空，之後職事表有資料時再補抓。

### 設計

1. 「建立本季週報」不再因為職事表找不到而中止。改為建立成功，
   事奉相關欄位留空
2. `BulletinWeeks` 加一欄 `ROSTER_STATUS`：`OK` / `NOT_FOUND` / `PARTIAL`
3. UI 頂部黃色橫幅：
   `本季職事表未有資料，事奉欄位全部留空。職事表建立好之後，撳「從職事表補抓」。`
4. 新增按鈕「**從職事表補抓**」：
   - 隨時可以重試
   - **只填空白格**，不覆寫人手已填的值（沿用現有三向比對）
   - 完成後顯示補了多少格、仍有多少格空白
5. 完成度自我檢測加一項：列出仍是 `NOT_FOUND` 或 `PARTIAL` 的季度
6. 星期一自動寄出：職事表找不到時**不要當成錯誤**，
   改為寄出但在信中註明「本期事奉資料尚未確定」
   （現時會拋 `ROSTER_NOT_FOUND` 並中止，`ErrorLog` 已有一筆 2026-08-24 的紀錄）

---

## R-037　示範與正式上線前的環境整理　【已完成】（2026-08-27）

### 要處理的

| 項目 | 處理方式 |
|---|---|
| 測試季度 `2027T4` 的資料 | 不刪，設 `ARCHIVED=TRUE`（見 R-035） |
| 沙盒季度 `2028T4` | 保留，自測機要用；確認永遠不會出現在使用者的季度下拉 |
| `PublishLog` 內 `IS_SELFTEST=TRUE` 的行 | 頂部狀態列與正式報表要排除，只計正式發佈 |
| 沙盒 master 檔案 | 保留，但確認 `SELFTEST_MASTER_PDF_FILE_ID ≠ PUBLISHED_PDF_FILE_ID` 的斷言仍在 |
| `Recipients` | 補齊 CC、DB、IT（R-029） |
| `DRY_RUN` | 示範時 `TRUE`；正式上線當日才改 `FALSE` |
| 觸發器 | 確認只安裝一個星期一觸發器，沒有重複 |
| `ErrorLog` | 示範前清一次，或至少確認最近 7 日沒有舊錯誤殘留 |
| 內容表樣本資料 | 示範用的內容表要另建一個，不要用 `2027T4` 那個 |

### 最重要的一次示範：由新季度從零開始

要寫一份 `docs/示範腳本.md`，逐步列明：

```
1. 建立本季週報（由職事表抓事奉資料）
2. 建立本季內容表
3. 寄內容表連結給 CC、DB
4. （堂委填內容表）
5. 從內容表匯入
6. 在 UI 補填講員、講題、讀經
7. 產生 Word → 用 Word 核對 → 另存 PDF
8. 發佈到 master 連結
9. 寄出通知
10. 下一個星期一，觸發器自動寄預覽給 CC、DB
```

每一步寫明：去邊度、撳咩、預期見到咩、失敗時點算。
這份腳本同時是你向堂委會、執事會簡報時的流程。

---

## 實作次序建議

1. **R-036**（職事表無資料仍可建立）——影響 12 月真數據演練，最優先
2. **R-034**（兩層主日選單）——使用者測試最直接的抱怨來源
3. **R-030**（夏令時間）——細，順手做
4. **R-033**（預覽連結）——中等，要決定 PDF 定 .docx
5. **R-035**（自動封存）——要小心，涉及隱藏資料
6. **R-037**（示範腳本）——上線前
7. **R-032**（分頁規則）——等真檔案，最後做

---

# 第三批：R-038 至 R-047 ＋ D 系列（2026-09-08 實測發現）

⚠️ **執行時機：示範之後、Word 範本那一輪之後。** 由
`prompt-R039-047-fill-ui-and-content-sheet-v2.md` 定義。除 R-038 已完成之外，
其餘**全部未開始**——Ivan 明確講「開始做 R-039」才動手。

⚠️ 逐條前提已經在 2026-09-08 對過原始碼，核對結果與**四項與 prompt 不符
的發現**寫在 `docs/待確認事項.md` CC 節。動手之前一定要先讀那一節。

## 貫穿全部的設計決定：第三種欄位模式

現時只有兩種：**介面填**（講題、序樂）與**內容表接管唯讀**
（宣召、十二個人數欄，見 `CONTENT_SHEET_READONLY_FIELDS`）。

要加第三種：**內容表為來源，介面可覆寫**。規則照抄 `src/DutyOverride.gs`
檔頭那五條，一條都不改：

1. 覆寫**只存週報**，不寫任何外部系統
2. 沒有覆寫的欄位，**每次匯入自動跟隨內容表最新值**
3. 有覆寫的欄位，**匯入不會蓋過去**，只在差異報告標 `CONFLICT`
4. 一切分歧**只提醒，不自動修正任何一邊**
5. 放棄覆寫用勾選對話框把 `ACTIVE` 改 `FALSE`，記錄保留

新增 `FieldOverride` 工作表，與 `DutyOverride` 同構：

```
主日日期 SERVICE_DATE | 欄位鍵 FIELD_KEY | 覆寫值 OVERRIDE_VALUE |
覆寫時的內容表值 SOURCE_VALUE_AT_OVERRIDE | 覆寫時間 OVERRIDE_AT |
覆寫者 OVERRIDE_BY | 原因 REASON | 有效 ACTIVE | 備註 NOTES
```

⚠️ 取值次序**固定**是「`BulletinWeeks` 現值 ← 內容表匯入 → 套用
`FieldOverride`」，不可以調轉，而且**只可以有一個地方**決定最終值。

## 需求一覽

| 編號 | 需求 | 狀態 |
|---|---|---|
| R-038 | 幹事的整條例行流程要能在填寫介面完成（見 `docs/需求登記.md` C 節） | 進行中（等人手測試） |
| R-039 | 事奉框加「已覆寫」視覺標記——覆寫過與未覆寫的格子現時外觀完全一樣 | 未開始 |
| R-040 | 下週事奉由唯讀改為可覆寫；同一格在兩個畫面出現，必須寫同一筆記錄 | 未開始 |
| R-041 | 「誦讀（覆寫）」下拉旁顯示目前自動值（由後端算好回傳，前端不可以重寫月份分組） | 未開始 |
| R-042 | `FLOWER_THIS_WEEK`／`FLOWER_NEXT_WEEK` 兩個週欄位接上職事表 `FLOWER` 崗位，用 `DutyOverride` | 未開始 |
| R-043 | 內容表新增「崇拜程序」分頁：讀經經文、證道講題、回應詩歌、詩班曲名，四欄用第三種模式 | 未開始 |
| R-044 | 三個詩歌欄位分工：`HYMN_PRAISE` 維持介面填、`CHOIR_LABEL` 加 Config 預設值（**渲染時 fallback，不寫入資料**）、`CHOIR_TITLE` 入內容表 | 未開始 |
| R-045 | 內容表新增「浸禮合堂」分頁，六欄用第三種模式；非浸禮主日全空不算錯誤 | 未開始 |
| R-046 | 「讀經」有歧義：崇拜程序那個改「讀經經文」、事奉框那個改「讀經（事奉）」；**Word 輸出文字不變** | 未開始 |
| R-047 | 「熱烈歡迎來賓」重複：範本硬寫 ＋ 內容表家事報告會再填一次 | 未開始（(b) 已選定，理由見待確認事項 CC-8） |

## 顯示與措辭修正（D 系列，全部 2026-09-08 實測發現）

| 編號 | 現況 | 要改成 | 狀態 |
|---|---|---|---|
| D-2 | 補抓對話框同時寫「那幾個主日在職事表仍然找不到」與「NOT_FOUND 0」 | 分清「季度找不到」與「該季指派是空的」，兩件事用不同措辭 | 未開始 |
| D-4 | 內容表「有效」欄留空被當成 `TRUE`，沒有任何提示 | 留空一律在差異報告明確列出，由幹事決定 | 未開始 |
| D-6 | 三份文件寫住「88 期之中有三期第 5、6 頁是複製漏刪」 | ⚠️ 與 `待確認事項.md` Y-5「已由 Ivan 用 Word 核實」**直接相反**，未改，見 CC-9 | 待 Ivan 決定 |
| D-7 | 補抓永遠報「仍有 12 格空白」，其實是平常主日的 `SPECIAL_TYPE` 本來就該空白 | 把「本應空白」與「缺資料」分開計算 | 未開始 |
| D-8 | 分歧報告出現兩行「影音 #1」（`SOUND` 與 `PPT` 同屬 `MG_AV`） | 合併組要顯示得出原始崗位，例如「影音 #1（音響）」 | 未開始 |
| D-9 | 儲存後頂部徽章更新了，主日下拉的「待填 N 項」沒有跟住更新 | 儲存成功後一併刷新下拉標籤 | 未開始 |
| D-10 | 獻花、翻譯這類「正常空白」永遠清不掉待填 | ⚠️ **要 Ivan 決定**，兩個做法見 CC-7 | 待 Ivan 決定 |
| D-11 | 「警告 N 項」徽章沒有 `stat-clickable`、沒有 title | 加成可撳，與「待填」「衝突」一致 | 未開始 |

## 實作次序建議（動手時再確認）

1. **R-046 ＋ D-11 ＋ D-9**——純顯示，無資料流改動，最安全，先做建立信心
2. **D-2 ＋ D-7 ＋ D-8**——措辭與計數，只碰報告，不碰資料
3. **第三種欄位模式 ＋ `FieldOverride`**——底層，R-043／R-044／R-045 全部靠它
4. **R-043 ＋ R-045**——內容表新分頁（要能對既有檔案「刷新」，不可以重建）
5. **R-044**——先答 CC-4 那條「左欄標題到底邊一個來源」
6. **R-040 ＋ R-042**——`DutyOverride` 擴到下一個主日，最容易寫錯，留最後
7. **R-039 ＋ R-041**——收尾的視覺與提示
8. **R-047 (b)**——連同重複偵測改為忽略編號前綴
